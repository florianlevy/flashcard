import { createMuiTheme } from '@material-ui/core'

export const FlashCardTheme = createMuiTheme({
  palette: {
    primary: {
      main: '#3445C7'
    },
    secondary: {
      main: '#23A840'
    }
  }
})
